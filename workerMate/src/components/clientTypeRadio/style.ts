import { StyleSheet } from 'react-native';

export const ClientTypeRadioStyle = StyleSheet.create({
    container: {
        flexDirection: 'row',
        width: '80%',
        justifyContent: 'space-between',
        paddingHorizontal: 20,
    },
    content: {
        flexDirection: 'row', 
        alignItems: 'center'
    }
});